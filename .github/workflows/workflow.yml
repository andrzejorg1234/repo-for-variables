# This is a basic workflow to help you get started with Actions

name: passing job values

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
  
jobs:
  pass_in_one_job:
    runs-on: ubuntu-latest
    steps:
      - name: Set color
        id: color-selector
        run: echo "COLOR=blue" >> $GITHUB_ENV
      - name: Get color
        env:
          COLOR: ${{ steps.color-selector.outputs.COLOR }}
        run: echo "The selected color is ${COLOR}"
        
  producer:
    runs-on: ubuntu-latest
    steps:
      - name: Make a sentence
        id: sentence_maker
        run: |
            echo "message=Hello, world!" >> $GITHUB_ENV

  consumer:
    runs-on: ubuntu-latest
    steps:
      - name: Print the message
        run: |
          echo ${jobs.producer.outputs.message}

  job_1:
    name: Add 3 and 7
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: |
          expr 3 + 7 > calc_result.txt
      - name: Upload result
        uses: actions/upload-artifact@v3
        with:
          name: calc_result
          path: calc_result.txt

  job_2:
    name: Multiply by 9
    needs: job_1
    runs-on: windows-latest
    steps:
      - name: Download result
        uses: actions/download-artifact@v3
        with:
          name: calc_result
      - shell: bash
        run: |
          value=`cat calc_result.txt`
          echo $(expr $value \* 9) 